.main-chat
  .main-chat__header
    .main-chat__header__group-info
      .main-chat__header__group-info__member-list
        Member：
        .main-chat__header__group-info__member-list__member
          -# - @group.users.each do |user|
          -#   =user.name

    -# =link_to 'Edit',edit_group_path(@group), class: "main-chat__header__group-info__edit"
  = form_with model: @messages, local: true do |f|
    .main-chat__list
      .first-box
        .first-box_question
          = Question.find(1).name
        .first-box_answer
          = f.collection_select :first_answer,Yesno.all, :id, :name,{include_blank: "選択してください"},  {class: "details_select"}

        .answer
          = f.submit "回答する", class: "answer-box"
          -# .yes-answer
          -#   YES/
          -# .no-answer
          -#   NO

      .second-box
        - if @first == 1
          .second-box_question
            あなたは今活動的ですか？
        - else

          .second-box_anser
            NO
      
      .third-box
        .third-box_question
          あなたは今朝起きるの辛いですか？
        .third-box_anser
          YES

      .article-proposal
        あなたに最適な記事はこちら！！
        %br
        - if @first_answer + @second_answer + @third_answer == 0
          = @article.url
        - else
      -# = render @messages





  -# .main-chat__message-form
  -#   -# = form_with model: [@group, @message], html: {class: "new_message"}, local: true do |f|
  -#     .input-box
  -#       = f.text_field :content, class: 'input-box__text', placeholder: 'type a message'
  -#       = f.label :image, class: 'input-box__image' do
  -#         = icon('far', 'image', class: 'Form__icon')
  -#         = f.file_field :image, class: 'input-box__image__file'
  -#     = f.submit 'Send', class: 'submit-btn'