.answer
  .question
    Q.あなたと同じ悩みを抱えている人がいるとすると、
    %br あなたはどんな
    %b "すとれすけしごむ"
    を渡しますか？
  .selected-trouble
    -if @message.category_id == 1
      「 職場 」
    -if @message.category_id == 2
      「 学校 」
    -if @message.category_id == 3
      「 教育関係（保護者目線） 」
    -if @message.category_id == 4
      「 恋愛 」
    -if @message.category_id == 5
      「 家庭 」
    -if @message.category_id == 6
      「 隣人 」
  = form_with model: @message, local: true do |f|
    = f.text_area :memo, class: 'answer-form', placeholder: 'type a comment'
    .answer-btn
      = f.submit "answer", class: "answer-box"

